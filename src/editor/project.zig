const model = @import("project/model.zig");

pub const Tool = model.Tool;
pub const ColorChannel = model.ColorChannel;
pub const ProjectMode = model.ProjectMode;
pub const PaletteColor = model.PaletteColor;

pub const MAX_MAP_W = model.MAX_MAP_W;
pub const MAX_MAP_H = model.MAX_MAP_H;
pub const MAX_MAP_CELLS = model.MAX_MAP_CELLS;
pub const MAX_MAP_SPRITES = model.MAX_MAP_SPRITES;

pub const MapSize = model.MapSize;
pub const TILE_FLAG_TRAVERSABLE = model.TILE_FLAG_TRAVERSABLE;
pub const TILE_FLAG_SLOW = model.TILE_FLAG_SLOW;

pub const MapTileAttr = model.MapTileAttr;
pub const MapSprite = model.MapSprite;
pub const MapCell = model.MapCell;
pub const Map = model.Map;
pub const Image = model.Image;
pub const Tile = model.Tile;
pub const Project = model.Project;
